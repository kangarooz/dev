# Core (no ML). Install with: pip install -r requirements.txt
playwright>=1.45
numpy>=1.24
soundfile>=0.12
imageio-ffmpeg>=0.5
sounddevice>=0.4   # record-ref (PortAudio; Linux: apt install libportaudio2). ffmpeg is the fallback
